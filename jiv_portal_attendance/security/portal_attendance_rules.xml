<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="jiv_attendance_portal_rule" model="ir.rule">
        <field name="name">Portal: own attendance only</field>
        <field name="model_id" ref="hr_attendance.model_hr_attendance"/>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="domain_force">
            [('employee_id.user_id', '=', user.id)]
        </field>
    </record>

    <record id="jiv_hr_employee_portal_rule" model="ir.rule">
        <field name="name">Portal: read own employee record</field>
        <field name="model_id" ref="hr.model_hr_employee"/>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
        <field name="domain_force">[('user_id', '=', user.id)]</field>
    </record>

</odoo>
